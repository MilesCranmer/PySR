name: Windows

on:
  push:
    branches:
      - 'master'
    paths:
      - '**'
    tags:
      - 'v*.*.*'
  pull_request:
    branches:
      - 'master'
    paths:
      - '**'

jobs:
  test:
    uses: ./.github/workflows/reusable.yml
    with:
      os: windows-latest
      python-version: '3.13'
      julia-version: '1'
      extra-tests: |
        pip install torch
        python -m pysr test torch
